numpy>=1.21
pandas>=1.3
matplotlib>=3.4
scikit-learn>=1.0   # only needed for fit_models.py (re-fitting the predictive models)
openpyxl>=3.0       # only needed to read the .xlsx dataset in fit_models.py
